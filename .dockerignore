# Dockge-PVE Docker build context

.env
node_modules
.idea
data
stacks
tmp
/private

# Generated frontend output must always come from the build stage
frontend-dist

# Git metadata
.git
.gitignore

# Docker build definitions are not needed inside the image filesystem
docker

# Documentation not needed at runtime
README.md
